<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Guards the command receiver inside DeskClock; only apps signed with
         our certificate (i.e. this app itself) can trigger it. -->
    <permission
        android:name="com.banana.hypermodes.permission.CONTROL"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.banana.hypermodes.permission.CONTROL" />

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Driving detection: car Bluetooth + motion (Activity Recognition) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Re-register driving detection after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Scheduled modes: exact alarms fire on time even in doze -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Lets the UI offer the battery-optimization exemption prompt -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <package android:name="com.android.deskclock" />
        <package android:name="com.mi.health" />
        <!-- Lets the app-exceptions picker list launchable apps. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:description="@string/xposed_description"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HyperModes">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="uiMode|fontScale|locale|layoutDirection|density"
            android:theme="@style/Theme.HyperModes">
        </activity>

        <activity-alias
            android:name=".LauncherAlias"
            android:enabled="false"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:targetActivity=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
    </application>

</manifest>
