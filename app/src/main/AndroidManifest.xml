<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Guards the command receiver inside DeskClock; only apps signed with
         our certificate (i.e. this app itself) can trigger it. -->
    <permission
        android:name="com.banana.hypermodes.permission.CONTROL"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.banana.hypermodes.permission.CONTROL" />

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Driving detection: car Bluetooth + motion (Activity Recognition) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Re-register driving detection after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Scheduled modes: exact alarms fire on time even in doze -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Lets the UI offer the battery-optimization exemption prompt -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <package android:name="com.android.deskclock" />
        <package android:name="com.mi.health" />
        <!-- Lets the app-exceptions picker list launchable apps. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:description="@string/xposed_description"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HyperModes">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HyperModes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Driving detection: car Bluetooth connect/disconnect (system broadcast) -->
        <receiver
            android:name=".driving.BluetoothDrivingReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
        </receiver>

        <!-- Driving detection: Activity Recognition IN_VEHICLE transitions -->
        <receiver
            android:name=".driving.ActivityTransitionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.banana.hypermodes.DRIVING_ACTIVITY_TRANSITION" />
            </intent-filter>
        </receiver>

        <!-- Re-register driving detection after reboot -->
        <receiver
            android:name=".driving.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Official bedtime state pushes from the DeskClock hook (sender is
             DeskClock's uid, so this must be exported; no signature permission
             is possible since DeskClock can't hold ours). -->
        <receiver
            android:name=".receiver.BedtimeStateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.banana.hypermodes.BEDTIME_ACTIVE" />
            </intent-filter>
        </receiver>

        <!-- Mode engine: alarm triggers + reschedule (our own app only) -->
        <receiver
            android:name=".engine.EngineReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.banana.hypermodes.ALARM_TRIGGER" />
                <action android:name="com.banana.hypermodes.RESCHEDULE" />
            </intent-filter>
        </receiver>

        <!-- Re-arm schedule alarms when the wall clock changes (protected
             broadcasts — only the system can send them). -->
        <receiver
            android:name=".engine.TimeChangedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
